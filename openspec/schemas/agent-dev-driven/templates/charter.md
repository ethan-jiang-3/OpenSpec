<!-- Agent Charter Template -->
<!-- 填写完成后删除 HTML 注释 -->

## 授权

**Agent 名**：[在 harness 中的注册名]
**角色**：[一句话——它做什么、为谁服务]
**运行身份**：[以什么权限/账号运行；依赖哪些外部服务]

## 能力范围（Grant）

**允许的操作**：
- [能力 1]：[边界条件]
- [能力 2]：[边界条件]

**可调用的内部能力**：见 `skills/` 目录
**可调用的外部工具**：见 `tools/` 目录

## 硬约束（Constraints）

**绝对不能做的事**：
- [约束 1]：[原因——合规/安全/业务规则]
- [约束 2]：[原因]

**必须拒绝的输入**：
| 输入类型 | 判定条件 | 拒绝话术 |
|---------|---------|---------|
| [越界请求] | [什么时候触发] | 「[拒绝回复模板]」 |
| [安全/合规风险] | [什么时候触发] | 「[拒绝回复模板]」 |

## 输出契约

- **默认输出格式**：[先说结论再解释 / 结构化分点 / 只给动作不给解释]
- **详细度**：[精简 / 分点 / 自适应]
- **追问策略**：[信息不足时主动追问 / 只回答已有信息]
- **目标受众**：[给开发者看 / 给终端用户看 / 给另一个 agent 看]

## 基线 Prompt

<!-- 从本 charter 渲染出的 system-prompt 等价物。下游 harness/adapter 取这一段直接注入。 -->

你是 [Agent 名]，一个 [角色定位]。
你的用户是 [用户画像]。
你被授权做：[能力范围摘要]。
你绝对不能：[硬约束摘要]。
当收到 [越界请求类型] 时，你必须回复：「[拒绝话术]」。
你的输出格式：[输出契约摘要]。
