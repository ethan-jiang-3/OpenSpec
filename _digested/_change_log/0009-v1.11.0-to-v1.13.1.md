# 同步 0009：v1.11.0 → v1.13.1

**日期**：2026-08-31

**upstream 范围**：`a0ddb60d040c61f4907436a9d91310934b1dda63`（v1.11.0）→ `bae58cf61479986431bb798acbe5a688a591c18c`（upstream/main）

**提交数量**：74（含 3 个 Version Packages 发版提交）

**文件统计**：250 files changed，23186 insertions，3129 deletions

**发布标签**：`v1.12.0`（09-03，Findings 报告 & SourceCraft）、`v1.13.0`（09-09，Apply 警告 & archive 大修）、`v1.13.1`（09-17，Hardened CLI, safer archives）。tag 后增量仅 1 个 docs 提交（`bae58cf6`，Docslab 链接修复），一并合入。

**本地合入**：`ethan` 以非快进 merge 合入 `upstream/main`。本次网络恢复可直连 git 协议，不再需要 0008 的 tarball+graft 方案；merge-base 为 `1ebddd17`（v1.10.0），33 个内容冲突全部为「ours=v1.11.0 vs theirs=v1.13.1」版本差，一律取 theirs 解决。本地资料（`_digested/`、`_faq_on_digested/`、`_openspec_handbook/`、`CONTEXT.md`）三方合并自动保留，无需 restore。Merge commit `c82a60c3`。

**工具链注意**：上游把 `packageManager` 从 `pnpm@9.15.9` 升到 `pnpm@10.34.5`。corepack 首次运行需要下载新版 pnpm；沙箱内需 `COREPACK_HOME=/tmp/corepack-home`，用户终端无感。

## 三个版本的核心主题

- **v1.12.0 — Findings 报告与规划落地**：`validate --report findings` 精简报告；新工具 SourceCraft（VS Code 扩展，tool id `codeassistant`）；propose/ff 变成 code-grounded（写 artifact 前先看代码、测试、文档）；explore 问依赖感知的问题、先查仓库再问用户。
- **v1.13.0 — archive/delta parser 正确性大修**：修了一批「写了但没生效、还报成功」级别的 bug（见下）。
- **v1.13.1 — 安全加固与工作流打磨**：敌意仓库输入加固；`status` 给 `Next:` 行；skills 只提 profile 内的 workflow；workflow 写文件前确认项目已 init；`/opsx:update` 先起草确认后写入；artifact 模板带顶层标题。

## 按领域拆解

### 新命令面

| 变更 | 版本 | 说明 |
|------|------|------|
| `validate --report findings` | v1.12.0 | 配合 bulk scope（`--all/--changes/--specs`）只输出 findings 列表，保留完整统计与退出码 |
| delta merge-conflict 标记报告为 informational findings | v1.12.0 | `a4fcdbec`：不改退出码；文件系统读取错误与 spec 缺失区分 |
| `status` 尾部 `Next:` 行 | v1.13.1 | 直接命名推进 change 的下一条命令 |
| 新工具 `codeassistant`（SourceCraft） | v1.12.0 | `src/core/command-generation/adapters/codeassistant.ts` |

### Delta parser / archive 正确性（v1.13.0 为主，最重要的一批）

| 修复 | 提交 | 之前的行为 |
|------|------|-----------|
| 接受所有 CommonMark 列表标记（`*`/`+`/`-`） | `fadac3e1` | `*`/`+` 写的 REMOVED/RENAMED 被静默忽略：validate 过、archive 报成功、requirement 没动 |
| 应用每一个 delta section header | `8251763e` | 文件里两个 `## ADDED Requirements` 只应用一份，还照样归档 |
| 包裹换行的 scenario bullet 读为一个 bullet | `c170dc77` | 折行 bullet 被拆开误读 |
| requirement 名剥收尾 `#` | `4b5c07a0` | 带收尾 `#` 的名字匹配不上 |
| 拒绝畸形的 RENAMED 对 | `6e62b5d5` | — |
| 拒绝仅大小写不同的 requirement 名 | `767d63c9` | archive 现在显式拒绝 |
| 拒绝无正文的 scenario header | `db560a63` | validate 报错 |
| 报告 delta section 之外的 requirement | `a5bf5c68` | 写错位置的 requirement 之前被无视 |
| archive 拒绝 merge path 读不到的 delta 文件 | `e01ed070` | 静默漏应用 |
| fence 内保留空行 | `aedf4d0c` | 文档化代码样例（YAML/Python/fixture）里的连续空行每次 archive 被重写 |
| archive 前检查 bulk-archive 目标 | `e67ac47f` | — |
| 新 capability 创建 main spec | `92fb72d1` | workflows 层保证 |
| change 嵌套在 namespace 目录时可被报告 | `09a999bb` | `src/utils/nested-change.ts` |

### 工作流指令演进（影响生成 skills/commands 文本）

| 变更 | 版本 | 说明 |
|------|------|------|
| propose 先读项目 context 再规划 | v1.13.0 | `6d2dbe62`：无 OpenSpec root 时 propose 停止并提示 init，不静默创建 |
| propose/ff code-grounded | v1.12.0 | `98bf53e5`：起草前检查相关代码、测试、文档 |
| explore 列 spec inventory | v1.13.0 | `3915db76`：显式引导 `openspec list --specs` + store-aware 读取，不再把 in-flight changes 当 specs |
| explore 聚焦式 discovery 问题 | v1.12.0 | `b976106d` |
| explore 的 capture 请求即写入确认 | v1.13.1 | `4c369e02`：v1.11.0 的「单独 yes/no」演进为「用户明确要求 capture 即视为确认」；并在每个 handoff 点名 `/opsx:propose`、`/opsx:apply` |
| `/opsx:update` 先起草（step 4）确认后写入（step 5） | v1.13.1 | `fede536c` |
| workflow 写文件前确认项目已 `openspec init` | v1.13.1 | `9827762d`：绝不顺手创建 `openspec/` 目录 |
| 生成的 skills 只提 profile 内 workflow + 认自然语言（"do an openspec apply"） | v1.13.1 | `626269ed`、`5f5914e7` |
| apply 遇无 delta spec 的 change 警告 | v1.13.0 | `8ba4ac1b`：提示写 specs 或 `skip_specs: true` |
| init/update 点名 profile 缺的 workflow | v1.13.0 | `3c6d318b` |
| tasks checkbox 计数认所有列表标记 + 未识别标记算未完成 + 无 checkbox 警告 | v1.13.0/1.13.1 | `8fc65b7f`、`11a96915`、`09984b82`；新模块 `src/core/validation/task-checkboxes.ts` |
| artifact 模板带顶层标题 | v1.13.1 | `3312af47`：markdownlint 不再报 |

### 安全加固（v1.13.1，#1835）

- `config.yaml` 值不能再向 agent instructions 注入指令
- 恶意构造的文件不能再挂起 `openspec update` / `openspec archive`
- 仓库 `.npmrc` 不能再重定向 update 检查
- `fast-uri` advisory 修补（v1.12.0）；chalk 保持 Node 20 兼容（v1.12.0）
- 新模块：`src/telemetry/opt-out.ts`

### Init / update / config / store

- init 给空目录加 `.gitkeep`（v1.12.0，`0296401b`）；重跑 init 安全恢复缺失 marker
- init/update 共享 IDE restart 提示模块（`src/core/shared/ide-restart.ts`），workflow 被移除时也准确提示
- update 刷新已 drift 的生成文件（`d9e1a28c`）
- global config 不可解析时保持不动（`605d9e7a`）；EDITOR 支持带参数（`2ef6fbde`）
- legacy command 目录中的用户文件保留（`388d3447`）
- store：拒绝删除包含另一 store 的 store（`9f8dec5d`）；名为 `specs`/`changes` 的 store 不再被当 root（`208b5b55`）；`setup --no-init-git` 可在 git 仓库内运行（`5d221456`）
- bash completion 卸载时逐字节还原 `.bashrc`（`72bf7600`）；nix flake 安装 shell completions（`3b8e5b66`）

### 其他

- `schema validate` 校验 apply block 与声明 artifacts 的一致性（`7090e16d`）
- `list` 跳过无法解析的条目再计日期（`8146be55`）
- npm git 安装不再要求用户机器有 pnpm（`1bcdf1b0`）；pnpm overrides 移到 `pnpm-workspace.yaml`（`e4e112d9`）
- 文档：CONTRIBUTING.md、community showcase、PowerShell completion、capability retirement 指南

## 对 `_digested/` 的影响

| 子目录 | 影响程度 | 说明 |
|--------|----------|------|
| **`workflows/`** | 🔴 严重 | explore（spec inventory、capture 即确认、handoff 点名）、propose（context-first、code-grounded）、apply（无 spec 警告）、archive（parser 大修）、update（draft-then-write）、bulk-archive（目标检查） |
| **`internal-spec-driven/`** | 🔴 严重 | delta parser 语义变化（列表标记、多 section、scenario bullet、requirement 名规则、section 外 requirement） |
| **`spec_cli/`** | 🔴 严重 | `validate --report findings`、validate 预报 archive 拒收、`status` Next 行、新工具 `codeassistant`、init `.gitkeep`/缺 workflow 点名 |
| **`specs_truth/`** | 🟡 中等 | parser 修复直接改变「spec 失真」的方式——`*`/`+` 静默忽略这类失真源已消除 |
| **`mechanisms/`** | 🟡 中等 | 安全加固、`ide-restart.ts` 共享模块、`task-checkboxes.ts`、completions `shell-quote.ts`、telemetry opt-out |
| **`schema/`** | 🟢 低 | `schema validate` 校验 apply block 一致性 |
| **`system/`** | 🟢 低 | 无架构级变化 |
| **`_coverage/`** | 🟡 中等 | 新增 ~8 个 src 模块，矩阵需刷新 |

### 优先级建议

1. **第一优先**：`workflows/` + `internal-spec-driven/` — v1.13.0 的 parser 正确性和 v1.13.1 的工作流确认演进是本轮核心故事
2. **第二优先**：`spec_cli/` — findings 报告、status Next、codeassistant
3. **第三优先**：`mechanisms/` 安全加固 + `specs_truth/` 失真源更新
4. **可选**：`schema/`、`system/`、`_coverage/`

## 对 `_faq_on_digested/` 与 `_openspec_handbook/` 的影响

- FAQ：03（explore 确认演进为 capture 即确认）、06/07（apply 无 spec 警告、archive 正确性）、12（版本表）、16（升级提醒至 v1.13.1）
- handbook：版本 banner → v1.13.1；explore 守卫描述更新；`status` Next；archive 修复相关的漂移维护章节

## 验收基线

```text
git log --oneline -1        # merge commit c82a60c3
node -p "require('./package.json').version"  # 1.13.1
pnpm run build / pnpm test   # 全绿
openspec --version           # PATH 环境独立升级（用户终端 npm i -g）
```

## 当前处理状态

- [x] upstream 范围与行为审计完成
- [x] upstream 源码以非快进 merge 合入（直连 fetch，冲突取 theirs）
- [x] 本地资料三方合并自动保留
- [x] pnpm@10.34.5 依赖安装 + build 通过
- [x] `_digested/_change_log/0009` 记录（本文件）
- [x] `_digested/` 各子目录更新（workflows×5、internal-spec-driven、spec_cli、mechanisms）
- [x] `_faq_on_digested/`（03、16）、`_openspec_handbook/`（banner×12、00-index 版本表+记录行）更新
- [x] 测试验收回填：198 test files / 5745 tests 全通过

## 尾部资料更新补充（同日第二轮）

除上述已列项外，另完成：`specs_truth/02`（失真源变化）与 `06`（v1.13.x 锚点）、`mechanisms/05-cli-infra`（安全加固+新模块）、`spec_cli/01`（status Next、validate findings、init `.gitkeep`、update drift 刷新）、FAQ 06/07、handbook 09/12 实质性提示、`_coverage/src-coverage`（8 个新模块矩阵）、`schema/00-map`（apply block 校验）。
## 第二轮逐篇核对（同日，更正 + 结构性归位）

按「变了的原地改、新增的结构性归位」原则重过一遍后的增量：

**更正**：#1710 此前被误写为「预报 archive 会拒收的 delta」，实际是 delta 内 merge-conflict 标记作为 informational findings（不改退出码）+ 文件系统读取错误与 spec 缺失区分。已修正 0009 记录、spec_cli 01/04、FAQ 16 共 4 处。

**原地修正（过时断言）**：
- `internal-spec-driven/01-explore`：开头「不产生 artifact，不修改任何文件」——v1.13.0 上游专门修正该承诺（`b9281652`）；CLI 调用链补 spec inventory；新增聚焦式 discovery 节；捕捉确认语义；四方差异表 explore 行
- `internal-spec-driven/02-propose`：proposal 模板 v1.13.1 起以 `# Proposal` 顶层标题开头；instruction 新增 inventory-first 要求（`list --specs` + `show --json --no-scenarios` + 全文读 spec）；补 context-first / code-grounded 前提
- `internal-spec-driven/03-apply`：checkbox 章节全面更新——计数器（`task-progress.ts`）扩展为全 CommonMark 标记；新模块 `task-checkboxes.ts` 负责「无 checkbox」检测、`+` 与有序标记计数、`- [ x]` 算完成、无 checkbox 的任务列表 validate 警告
- `handbook/00-index`：explore「不生成 artifacts」绝对化描述修正

**结构性新增**：
- `system/06-源码地图`：validation 下补 `task-checkboxes.ts`/`purpose-placeholder.ts`，新增 `nested-change.ts`、`opt-out.ts`、`ide-restart.ts`、`optional-workflow.ts`、`project-root.ts`、`shell-quote.ts`、`codeassistant.ts` 七个模块行
- `spec_cli/02`：apply 警告字段、status Next（text-only）、findings JSON、顶层标题对 JSON 命名的影响
- `spec_cli/03`：status `Next:` 行；instructions apply 内部处理补计数器与警告
- `spec_cli/05`：config 防重写保护（#1876）；optional-workflow 生成时 handoff 解析；skill 自然语短语
- `spec_cli/07`：validate 行更新 + status Next 注
- `workflows/00-overview`：template 层四个新机制（optional-workflow / project-root guard / 顶层标题 / Next 行）
- `mechanisms/01-store`：store 三修复表；`mechanisms/02`：universal picker 命名 + 自然短语
- `handbook/99`：工具列表补 zed / SourceCraft / universal 命名
- `_coverage/tests-coverage`：v1.13.x 新增测试映射表

**核查后无需变更**：上游零文件删除；`openspec/specs/` 无新增 requirement header（specs-coverage 映射不变）；workflows 03-new/07-sync/08-verify/11-onboard 无行为变化；handbook 11 实战叙事不受影响。
