# 00 — 四条命令的共有机制

explore、propose、apply、archive 表面上做完全不同的事，但运行在同一套基础设施上。理解这套"共性层"才能看懂每条命令为什么那样工作。

---

## 1. OpenSpec 的 meta 本质：框架，不是工具

OpenSpec 不是一个"帮你写 spec"的工具。它是一个**告诉 AI agent 如何写 spec 的框架**。

打个比方：大多数工具是菜谱（告诉你放什么料、炒几分钟），OpenSpec 是厨房（提供灶台、锅铲、计时器，但菜谱你自己定）。

三层分工：

```
spec-driven schema (YAML + templates)  ──  "做什么菜、先后顺序、每个步骤干什么"
CLI (Commander.js, ~1000行 TypeScript) ──  "计时器响了没、食材在哪、锅热了没"（纯确定性计算）
AI Agent (Claude Code / Cursor 等)      ──  "实际切菜炒菜的人"（生成性工作）
```

**关键推论**：OpenSpec 的行为不是硬编码的。改 `schema.yaml` 和 `templates/` 就能改整个工作流，一行 TypeScript 都不用动。spec-driven 只是默认 schema —— 你可以写自己的。

---

## 2. 三方架构

每次 `/opsx:propose` 这样的调用，实际运转的是三方接力：

```
User（人类）
  │  "/opsx:propose add-dark-mode"
  ▼
Host Agent（Claude Code / Cursor / Cline）
  │  读取 SKILL.md 中的指令
  │  执行: openspec new change "add-dark-mode"
  │  执行: openspec status --change "add-dark-mode" --json
  │  执行: openspec instructions proposal --change "add-dark-mode" --json
  │  写文件: proposal.md, specs/*.md, design.md, tasks.md
  ▼
OpenSpec CLI（确定性内核）
  │  读 schema.yaml → 构建 artifact DAG
  │  读文件系统 → 检测哪些 artifact 已完成
  │  读 config.yaml → 提取 context / rules
  │  读 templates/*.md → 加载输出骨架
  │  组合成 structured JSON 返回给 agent
  ▼
文件系统（唯一事实源）
  openspec/changes/<name>/  — change 目录
  openspec/specs/           — 主 spec 基线
  openspec/config.yaml      — 项目配置
```

**CLI 只做确定性计算，从来不做生成**。它读文件、算状态、组合指令。文本内容是 AI agent 写的。

---

## 3. Schema：控制一切的"源码"

`schemas/spec-driven/schema.yaml`（154 行）是整个工作流的定义文件，等价于"源码"。它声明了：

### Artifact 定义

```yaml
artifacts:
  - id: proposal
    generates: proposal.md          # 输出文件路径
    description: "Initial proposal..."
    template: proposal.md           # 模板文件名（相对于 templates/）
    instruction: |                  # 给 AI 的指导
      Create the proposal document...
    requires: []                    # 依赖（空 = 立即可做）

  - id: specs
    generates: "specs/**/*.md"      # glob 模式匹配多文件
    template: spec.md
    instruction: |
      Create specification files...
    requires:
      - proposal                    # 必须等 proposal 完成

  - id: design
    generates: design.md
    template: design.md
    requires:
      - proposal                    # 只依赖 proposal（和 specs 并行）

  - id: tasks
    generates: tasks.md
    template: tasks.md
    requires:
      - specs                       # 必须等 specs 和 design 都完成
      - design
```

### DAG 可视化

```
         proposal (ready immediately)
           /          \
          ▼            ▼
        specs        design
          \          /
           ▼        ▼
            tasks
              │
              ▼
            apply (requires: [tasks], tracks: tasks.md)
```

### Apply Phase 定义

```yaml
apply:
  requires: [tasks]      # 哪些 artifact 完成后才能 apply
  tracks: tasks.md       # checkbox 文件，用来跟踪进度
  instruction: |
    Read context files, work through pending tasks...
```

`requires: [tasks]` 意味着 tasks.md 里哪怕还剩一个 `- [ ]`，apply 阶段都不算完成。

---

## 4. 文件系统即状态

OpenSpec 没有数据库，没有 `.state.json`，没有 lock file。**artifact 是否完成 = 它 generates 的文件是否存在于磁盘上**。

关键代码路径：`src/core/artifact-graph/state.ts:14-29`

```typescript
export function detectCompleted(graph: ArtifactGraph, changeDir: string): CompletedSet {
  const completed = new Set<string>();
  for (const artifact of graph.getAllArtifacts()) {
    if (artifactOutputExists(changeDir, artifact.generates)) {
      completed.add(artifact.id);
    }
  }
  return completed;
}
```

对于简单路径如 `proposal.md`：直接 `fs.existsSync(changeDir + '/proposal.md')`。

对于 glob 如 `specs/**/*.md`：用 `fast-glob` 匹配 —— **只要匹配到至少一个文件就算完成**（`src/core/artifact-graph/outputs.ts:40-42`）。

**这个设计的意义**：
- 状态和内容在同一个地方（文件系统），不会出现"数据库说完成了但文件不存在"的一致性问题
- agent 不需要学任何新概念，"写完文件"和"标记完成"是同一件事
- git diff 自然就能看到变化

---

## 5. 四层注入机制

当 agent 调用 `openspec instructions <artifact> --change X --json` 时，CLI 组装了四层信息。代码在 `src/core/artifact-graph/instruction-loader.ts:273-341`（`generateInstructions()`）。

### 四层结构

| 层级 | 来源 | 作用 | 给谁看 |
|------|------|------|--------|
| **context** | `config.yaml` 的 `context` 字段 | 项目背景信息（技术栈、领域术语等） | AI 自己看，**不写入** artifact |
| **rules** | `config.yaml` 的 `rules[artifactId]` | 该 artifact 特有的约束 | AI 自己看，**不写入** artifact |
| **instruction** | `schema.yaml` 的 `artifact.instruction` | 这个 artifact 应该包含什么、常见坑 | AI 参考，不要求逐字复制 |
| **template** | `templates/<name>.md` | 输出文件的结构骨架 | **这就是输出文件的格式** |

### 注入顺序

以下是四层在 `printInstructionsText()` 输出中的排列（简化视图，实际输出还包含 `<artifact>` 外层包装、`<task>`、`<dependencies>`、`<output>`、`<success_criteria>`、`<unlocks>` 等结构标签）：

```
<project_context>
  <!-- 这是背景信息，不要包含在你的输出中 -->
  Tech stack: TypeScript, React, Node.js
  We use conventional commits.
</project_context>

<rules>
  <!-- 这些是约束，不要包含在你的输出中 -->
  - Keep proposals under 500 words
  - Always include a "Non-goals" section
</rules>

<instruction>
  Create the proposal document that establishes WHY this change is needed.
  Sections: Why, What Changes, Capabilities, Impact...
</instruction>

<template>
  ## Why
  <!-- 解释此变更的动机 -->

  ## What Changes
  <!-- 描述什么会改变 -->
  ...
</template>
```

**关键区分**：context 和 rules 被包裹在 `<!-- -->` 注释中，并明确标注 "Do NOT include in your output"。它们是给 AI 的内部约束，不是给 artifact 的内容。template 才是输出骨架。

### 代码路径

`generateInstructions()` (`instruction-loader.ts:273`) 的执行顺序：

1. 从 graph 查找 artifact (`context.graph.getArtifact(artifactId)`)
2. 调用 `loadTemplate()` 加载模板文件内容
3. 调用 `getDependencyInfo()` 收集依赖项状态（哪些完成了、输出路径在哪）
4. 调用 `getUnlockedArtifacts()` 计算"完成本 artifact 后会解锁哪些"
5. 读取 `config.yaml`（resilient —— 读不到就跳过）
6. 验证 config 的 rules 字段中的 artifact ID 是否在 schema 中存在（无效 ID 产生 warning，每个 session 只警告一次）
7. 提取 `configContext`（来自 `config.context`）
8. 提取 `configRules`（来自 `config.rules[artifactId]`）
9. 返回 `ArtifactInstructions` 对象

最后 `printInstructionsText()` (`src/commands/workflow/instructions.ts:107-224`) 将 `ArtifactInstructions` 格式化为 XML-tagged 文本输出。

---

## 6. status + instructions：核心配合模式

这是四条命令共享的核心循环：

```
status   →  告诉 agent "该做什么"（哪些 artifact ready，哪些 blocked）
instructions → 告诉 agent "怎么做"（四层注入后的完整操作包）
```

### status 的输出 (`formatChangeStatus()`, `instruction-loader.ts:383-459`)

```json
{
  "changeName": "add-dark-mode",
  "schemaName": "spec-driven",
  "changeRoot": "/project/openspec/changes/add-dark-mode",
  "planningHome": { "kind": "repo", "changesDir": "...", "defaultSchema": "spec-driven" },
  "artifactPaths": {
    "proposal": { "outputPath": "proposal.md", "resolvedOutputPath": "...", "existingOutputPaths": [] },
    "specs":    { "outputPath": "specs/**/*.md", "resolvedOutputPath": "...", "existingOutputPaths": [] },
    ...
  },
  "artifacts": [
    { "id": "proposal", "outputPath": "proposal.md", "status": "ready" },
    { "id": "specs",    "outputPath": "specs/**/*.md", "status": "blocked", "missingDeps": ["proposal"] },
    { "id": "design",   "outputPath": "design.md", "status": "blocked", "missingDeps": ["proposal"] },
    { "id": "tasks",    "outputPath": "tasks.md", "status": "blocked", "missingDeps": ["specs", "design"] }
  ],
  "applyRequires": ["tasks"],
  "isComplete": false,
  "nextSteps": ["Run: openspec instructions proposal --change add-dark-mode"],
  "actionContext": {
    "mode": "repo",
    "sourceOfTruth": "openspec/",
    "allowedEditRoots": ["."],
    "constraints": ["Respect existing patterns", "Follow conventional commits"]
  }
}
```

关键字段：
- **`artifacts[].status`**: `done`（文件存在）| `ready`（依赖满足）| `blocked`（有未满足的依赖）
- **`applyRequires`**: 来自 schema 的 `apply.requires`，告诉 agent 哪些 artifact 全做完才能进入实施
- **`nextSteps`**: 纯文本建议，给人类和 agent 看
- **`actionContext`**: 机器可读的约束，给 agent 程序化使用

### instructions 的输出 (`generateInstructions()`)

每个字段的精确含义：

| 字段 | 含义 |
|------|------|
| `context` | config.yaml 的 context，AI 约束 |
| `rules` | config.yaml 的 rules[artifactId]，AI 约束 |
| `instruction` | schema.yaml 的 artifact.instruction，创建指导 |
| `template` | 模板文件的原始内容，**输出格式** |
| `dependencies` | 依赖项列表（id, done?, path, description） |
| `unlocks` | 完成后会解锁哪些 artifact |
| `resolvedOutputPath` | 文件应该写到哪的绝对路径 |
| `existingOutputPaths` | 已存在的输出文件（覆盖前先读） |

---

## 7. 关键 CLI 命令一览

| 命令 | JSON 支持 | 作用 | 谁调用 |
|------|----------|------|--------|
| `openspec list --json` | 是 | 列出所有活跃 change | explore/apply/archive |
| `openspec status --change X --json` | 是 | 获取 artifact DAG 状态 | 所有四条命令 |
| `openspec instructions <id> --change X --json` | 是 | 获取创建某 artifact 的完整指令 | propose |
| `openspec instructions apply --change X --json` | 是 | 获取实施的 work order | apply |
| `openspec new change X` | 否 | 创建 change 目录骨架 | propose |
| `openspec schemas --json` | 是 | 列出可用 schema | （辅助） |
| `openspec templates --json` | 是 | 列出某 schema 的所有模板路径 | （辅助） |

---

## 8. 双重交付机制

`openspec init` 根据 profile 和 delivery 设置生成两套文件（`src/core/init.ts:494`）：

### Skills（`SKILL.md`）

路径：`.claude/skills/openspec-<workflow>/SKILL.md`

每个 skill 是带 YAML frontmatter 的 Markdown 文件：
```markdown
---
name: openspec-propose
description: Propose a new change...
metadata:
  generatedBy: openspec
---
Propose a new change - create the change and generate all artifacts in one step.
...
```

Skills 是**主要机制** —— agent 通过 skill 名匹配来激活对应的行为指令。

### Commands（slash commands）

路径：`.claude/commands/opsx/<id>.md`

对于 Claude Code 等支持原生 slash command 的工具，额外生成 command 文件。内容与 skill 基本相同但格式适配各工具的约定。

### Profile 控制安装什么

`src/core/profiles.ts:14`：
```typescript
const CORE_WORKFLOWS = ['propose', 'explore', 'apply', 'sync', 'archive'];
```

**core profile**（默认）：5 个 workflow。也就是 `/opsx:explore`、`/opsx:propose`、`/opsx:apply`、`/opsx:sync`、`/opsx:archive`。

用户可切换到 custom profile，自定义 workflow 列表。

### 模板源文件

所有 skill/command 的内容模板在 `src/core/templates/workflows/*.ts`，每个 workflow 一个文件：

| 文件 | 导出函数 |
|------|---------|
| `explore.ts` | `getExploreSkillTemplate()`, `getOpsxExploreCommandTemplate()` |
| `propose.ts` | `getOpsxProposeSkillTemplate()`, `getOpsxProposeCommandTemplate()` |
| `apply-change.ts` | `getApplyChangeSkillTemplate()`, `getOpsxApplyCommandTemplate()` |
| `archive-change.ts` | `getArchiveChangeSkillTemplate()`, `getOpsxArchiveCommandTemplate()` |

---

## 9. 四条命令在这套基础设施上的差异

| | explore | propose | apply | archive |
|------|------|------|------|------|
| **创建 artifact** | 否 | 是（全部） | 否（只改 checkbox） | 否（只合并+移动） |
| **核心 CLI 调用** | list, status | new change, status, instructions (循环) | status, instructions apply | status, (sync), mv |
| **DAG 角色** | 旁观 | 按 DAG 顺序逐一创建 | 依赖 DAG 的 apply 阶段 | 消费 DAG 的最终产物 |
| **修改什么** | 无 | 创建 proposal/specs/design/tasks | 修改 tasks.md checkbox + 应用代码 | 合并 specs + 移动 change 目录 |
| **schema 依赖** | 弱（只读） | 强（控制创建顺序和内容） | 中（apply phase 定义） | 强（控制合并逻辑） |
