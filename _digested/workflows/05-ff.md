# Workflow · ff (fast-forward)

## 源文件

`src/core/templates/workflows/ff-change.ts` → `getFfChangeSkillTemplate()` + `getOpsxFfCommandTemplate()`

> **调用方式**：command adapter 可为 `/opsx:ff <change-name 或描述>`；Codex 用 `$openspec-ff-change`。下文的 `/opsx:` 仅表示前者。
> **agent 看到的名字**：`openspec-ff-change`（skill）/ `OPSX: Fast Forward`（command）
> **独立 CLI 命令**：无。
> **profile**：custom（需显式启用，不在默认 core 里）

## 一句话

ff 是 propose 的轻量版：共享同样的 status → instructions → write → status 循环直到 apply-ready，但不强制要求新 change——它可以在已有 change 上运行，语义更偏"快速补齐剩余 artifact"。

## 和 propose 的源码级对比

ff 的 template 和 propose 几乎完全相同。逐段对比：

| 段 | propose | ff |
|---|---|---|
| 输入处理 | 相同：无输入→向用户询问→导出 kebab-case | 相同 |
| new change | `openspec new change "<name>"` | 相同 |
| status | 相同 | 相同 |
| artifact 循环 | 相同：status→instructions→write→status | 相同 |
| apply gate | `applyRequires` 全部 done | 相同 |
| Guardrails | 相同 + "同名 change→问用户" | 相同 + "同名 change→建议 continue" |

唯一的语义差别在 guardrail：propose 对同名 change 的处理是 "ask if user wants to continue it or create a new one"，ff 是 "suggest continuing that change instead"——因为 ff 的语义更偏"推进已有 change"。

## CLI 命令调用序列

与 propose 相同：

```text
1. openspec new change "<name>"           # 或用于已有 change
2. openspec status --change "<name>" --json
3. loop:
   a. openspec instructions <artifact> --change "<name>" --json
   b. [agent 从磁盘重读依赖 + 写 artifact]
   c. openspec status --change "<name>" --json
   until applyRequires 全部 done
4. openspec status --change "<name>"
```

## 适用场景

| 场景 | 用什么 |
|---|---|
| 新 change，一口气完成 | propose |
| 新 change，先占位再逐步 | new → continue × N |
| **已有 change，artifact 不完整，想快速补齐** | **ff** |
| 已有 change，逐步推进 | continue |

## Guardrails

与 propose 相同，差异仅在 change 同名处理。

## 起草前先读项目代码

ff 与 propose 共享同一处引导补强（#1737）：起草 artifact 前检查相关项目代码、测试、文档——计划反映现有实现，而不是把基础发现推迟成实施任务。ff 的「快速补齐」语义下这点尤其重要：已有 change 的剩余 artifact 应该和代码现状对齐，而不是凭空补。

## 源码锚点

| 内容 | 行号范围（ff-change.ts） |
|---|---|
| SkillTemplate 定义 | L10-L110 |
| CommandTemplate 定义 | L113-L213 |


## v1.13.x 行为更新

- **code-grounded（`98bf53e5`，与 propose 共享）**：ff 起草 artifact 前先检查相关代码、测试与文档，计划落在既有实现上。
- **context-first（`6d2dbe62`）**：规划决策前先加载 project context；无 OpenSpec root 时停止并提示 init。
